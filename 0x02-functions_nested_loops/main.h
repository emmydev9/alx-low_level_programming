#include "stdio.h"
/**
* print_alphabet - print alphabets to the stdout
* @void: takes no argument
**/
int  _islower(int c)
{
	if (c >= 97 && c <= 122)
{
	return (1);
}
	else
{
	return (0);
}
	putchar('\n');

}
