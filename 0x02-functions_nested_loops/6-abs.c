#include "main.h"
/**
* _abs - print alphabets to the stdout
* @n: takes an integer argument
* Return: absolute value
**/
int  _abs(int n)
{
	if (n > 0)
		return (1);
	else if (n == 0)
		return (0);
	else
	return (n * -1);

_putchar('\n');
}
