#include <stdio.h>
#include <stdlib.h>

/**
* main - Entry points function
* @argc: number of arguments
* @argv: pointer to an array of char.
* Return: Always success dear
**/

int main(int argc, char __attribute__((__unused__)) *argv[])
{
	printf("%d\n", argc);
	return (0);
}
