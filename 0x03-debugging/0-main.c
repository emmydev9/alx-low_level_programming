#include "main.h"
/**
* main - Entry Points 
* @void: No arguments needed
* Return: return 0 on success
**/
int main(void)
{
		int i;

	if (positive_or_negative(i) == 0)
			printf("%d is zero", i);

	return (0);
}
