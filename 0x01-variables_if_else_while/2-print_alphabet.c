#include <stdio.h>
#include <stdlib.h>
/**
* main - Entry point function
* @void: No argument needed
* Return: returns 0 if success
**/
int main(void)
{
	char aph;

	for (aph = 'a'; aph <= 'z'; aph++)
		putchar(aph);

	putchar('\n')
	return (0);
}
